[
    {
      "name": "${name}",
      "image": "${ecs_image_name}:${image_tag}",
      "cpu": 10,
      "memory": 512,
      "essential": true,
      "portMappings": [
        {
          "containerPort": ${port},
          "hostPort": 0
        }
      ]
    }
]